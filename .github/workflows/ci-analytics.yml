# Independent CI pipeline for the Analytics micro-frontend.
# Triggers ONLY when analytics code changes — other modules don't run this.
# In a real multi-repo setup, this would be the repo's only CI workflow.

name: "CI: Analytics"

on:
  pull_request:
    branches: [main]
    paths:
      - "packages/analytics/**"
      - "vitest.config.ts"
      - "vitest.setup.ts"
      - "eslint.config.mjs"
      - "package.json"
  push:
    branches: [main]
    paths:
      - "packages/analytics/**"
      - "vitest.config.ts"
      - "vitest.setup.ts"
      - "eslint.config.mjs"
      - "package.json"

concurrency:
  group: ci-analytics-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: npm install --force
      - run: npx eslint packages/analytics/src

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: npm install --force
      - run: cd packages/analytics && npx tsc --noEmit

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: npm install --force
      - run: npx vitest run packages/analytics/src

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, typecheck, test]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: npm install --force
      - run: cd packages/analytics && npm run build
      - uses: actions/upload-artifact@v7
        with:
          name: analytics-dist
          path: packages/analytics/dist/
          retention-days: 7
