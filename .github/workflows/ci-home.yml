# Independent CI pipeline for the Home micro-frontend.
# Triggers ONLY when home code changes — other modules don't run this.
# In a real multi-repo setup, this would be the repo's only CI workflow.

name: "CI: Home"

on:
  pull_request:
    branches: [main]
    paths:
      - "packages/home/**"
      - "vitest.config.ts"
      - "vitest.setup.ts"
      - "eslint.config.mjs"
      - "package.json"
  push:
    branches: [main]
    paths:
      - "packages/home/**"
      - "vitest.config.ts"
      - "vitest.setup.ts"
      - "eslint.config.mjs"
      - "package.json"

concurrency:
  group: ci-home-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 11
          run_install: false
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: |
            pnpm-lock.yaml
            packages/*/pnpm-lock.yaml
      - run: pnpm install --force
      - run: pnpm exec eslint packages/home/src

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 11
          run_install: false
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: |
            pnpm-lock.yaml
            packages/*/pnpm-lock.yaml
      - run: pnpm install --force
      - run: cd packages/home && pnpm exec tsc --noEmit

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 11
          run_install: false
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: |
            pnpm-lock.yaml
            packages/*/pnpm-lock.yaml
      - run: pnpm install --force
      - run: pnpm exec vitest run packages/home/src

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, typecheck, test]
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 11
          run_install: false
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: |
            pnpm-lock.yaml
            packages/*/pnpm-lock.yaml
      - run: pnpm install --force
      - run: cd packages/home && pnpm run build
      - uses: actions/upload-artifact@v7
        with:
          name: home-dist
          path: packages/home/dist/
          retention-days: 7
